# Your API key. Never commit this.
.env

# Credit ledger + fetched odds — regenerated, and the quota file is
# specific to your key.
.odds-quota.json

.DS_Store
.gstack/

# Local agent state, not project code.
.omc/
nfl-history.json
cfb-history.json
.espn-athletes.json
# Built by `node tendencies.mjs` from the play-by-play cache: 3.9 MB for
# 2024-2026, which is over the 1 MB pre-commit ceiling and regenerates in
# three seconds. Committing it is a later decision, once something reads it.
tendencies-nfl.json

nflverse/
cfbfastr/
.playwright-mcp/
